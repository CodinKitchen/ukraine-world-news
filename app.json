{
  "scripts": {
    "dokku": {
      "postdeploy": "./post-deploy.sh"
    }
  },
  "cron": [
    {
      "command": "./post-deploy.sh && php bin/console app:generate-screenshots",
      "schedule": "0 */2 * * *"
    }
  ]
}
